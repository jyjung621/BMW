	<%
		session.setAttribute("memberId", "admin1");

		session.setAttribute("grade", "1");

		
		

		String memberId = (String)session.getAttribute("memberId");
		String Agrade = (String)session.getAttribute("grade");

		

	%>